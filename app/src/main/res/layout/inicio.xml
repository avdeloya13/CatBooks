<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/md_theme_onPrimary"
    android:fitsSystemWindows="true"
    android:orientation="vertical"
    android:id="@+id/main">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:background="@color/md_theme_primary"
            app:title="">

            <TextView
                android:id="@+id/app_name_toolbar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/app_name"
                android:textSize="40sp"
                android:textColor="@android:color/white"
                android:fontFamily="@font/yellowtail_regular"
                android:layout_gravity="start" />

        </androidx.appcompat.widget.Toolbar>

        <ImageView
            android:id="@+id/imageViewlogo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:srcCompat="@drawable/cat_img_logo_" />

        <!-- En esta actividad van a ir todos los libros de la tienda, con una lista personalizada que muestre la protada del libro, su titulo y autor.
       Pero como no nos han pedido utilizar listas para esta tarea me enfoque en lo que si nos pedian y solo
       le agregue la imagen del logo por lo mientras :)

       Pienso utilizar la misma estructura que hice en las vistas de la tarea 2:
       constraint -> scroll ->layout
       Pero le he quitado el scroll porque por el momento porque solo tengo la imagen.-->

    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>